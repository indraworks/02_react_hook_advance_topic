import React, { useReducer, useState } from "react"
import Modal from "./Modal"
import { data } from "../../../data"

const childIndex = () => {
  const [firstName, setFirstName] = useState("")
  return <div>childIndex</div>
}

export default childIndex
